# IIS-PHP-FastCGI-AutoDeploy
PowerShell script to automate the setup of IIS with PHP (FastCGI) for a custom domain on Windows 11 / Server. Checks prerequisites, installs IIS components, downloads and configures PHP, creates application pool and website, and sets up FastCGI handler. Ideal for deploying Roundcube or any PHP webmail.
